eureka:
  client:
    register-with-eureka: false

spring:
  application:
    name: gateway
  profiles:
    active: docker
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
      routes:
        - id: komentet
          uri: http://komentet:8071
          predicates:
            - Path = /api/v1/komentet/**

        - id: lajmet
          uri: http://lajmi:8090
          predicates:
            - Path = /api/v1/lajmet/**
server:
  port: 8222